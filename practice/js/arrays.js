// Different Types of Arryas
let intArr = [1, 2, 3];
let stringArr = ["Good", "Morning", "People", "monkey", "cat", "dog"];
let mixArr = [1, "good", 24.5];

// Printing Arrays
// console.log(intArr);
// console.log(stringArr);
// console.log(mixArr);

// Array Funcitons or Methods
let pets = ["bird", "cat", "dog", "ants", "monkey", "horse"];
// what is the range of the array? 6 (Index starts from 0 and ends at 6)
// console.log(pets.length); // what is the length of the array ? 7 (The number of elements in the array is 7)

// pets.shift(); // deletes the first element of the array
// pets.pop(); // deletes the last element of the array
// pets.push("donkey", "horse"); // adds an element at the end of the array
// pets.unshift("donkey"); // adds an element at the beginning of the array
// pets[1] = "lizard"; // changing the value of an element in the array
// let slice = pets.slice(0, 3);
// pets.splice(0, 3);
// console.log(pets.slice(0, 3)); // deletes the selected elements from the array but returns a new array

// pets.splice(3, 5); // deletes the selected elements from the original array

console.log(pets);

// Printing Arrays One by One
