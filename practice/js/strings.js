let changed = "";
let string = "tom and jerry";
let trailSpaces = "    tom and jerry   ";
let string1 = "tom,dick,harry";

// changed = string.toUpperCase(); // to convert the string to uppercase
// changed = string.toLowerCase(); // to convert the string to lowercase
// changed = string.replace("tom", "Tom"); // replaces a single word in the given value
// changed = string.length; // displays the length of the given string
// changed = string.split(); // splits the string into an array
// changed = string.split(" "); // splits the string into an array based on the delimiter given
// changed = string1.split(","); // splits the string into an array based on the delimiter given
// changed = string.charAt(0); // treats the entire string as an array and defines each letter as an index
// changed = string.concat(" and spike"); // concats the given parameter to the original value
// changed = string.startsWith("tom"); // checks if the given string starts with the given parameter
// changed = string.endsWith("jerry"); // checks if the given string ends with the given parameter
// changed = string.includes("errs"); // checks if the given parameter is part of the original value or not
// changed = string.slice(4, 7); // slices the given value to the given parameters
// changed = string.substring(0, 5); // slices the given value to the given parameters
// changed = trailSpaces.trim(); // removes all the trailing spaces both front and back for the given word
// changed = string.indexOf("i"); // displays the index of the first letter of the given parameter, if the parameter does not exist in the given word then, it returns a negative value

console.log(changed);
